name: Deploy

on:
  push:
    branches: [main]
  release:
    types: [published]

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      api: ${{ steps.filter.outputs.api }}
      web: ${{ steps.filter.outputs.web }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            api:
              - 'apps/api/**'
            web:
              - 'apps/web/**'

  deploy-api-preprod:
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      
      - name: Deploy API Preprod
        working-directory: apps/api
        run: |
          bun x vercel deploy \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --name=church-sas-api-preprod \
            --yes

  deploy-api-prod:
    needs: changes
    if: github.event_name == 'release' && needs.changes.outputs.api == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      
      - name: Deploy API Prod
        working-directory: apps/api
        run: |
          bun x vercel deploy \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --name=church-sas-api-prod \
            --prod \
            --yes

  deploy-web-preprod:
    needs: changes
    if: github.event_name == 'push' && needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      
      - name: Deploy Web Preprod
        working-directory: apps/web
        run: |
          bun x vercel deploy \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --name=church-sas-web-preprod \
            --yes

  deploy-web-prod:
    needs: changes
    if: github.event_name == 'release' && needs.changes.outputs.web == 'true'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: oven-sh/setup-bun@v2
      
      - name: Deploy Web Prod
        working-directory: apps/web
        run: |
          bun x vercel deploy \
            --token=${{ secrets.VERCEL_TOKEN }} \
            --name=church-sas-web-prod \
            --prod \
            --yes